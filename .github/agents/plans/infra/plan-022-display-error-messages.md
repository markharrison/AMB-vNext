error handle so that the interface returns dummy data if there are issues connecting to database, 
  also put the error in a header bar on the page explaining the error in detail and referencing the file and code line that has the error 
  but dont show the code itself.

  explain in detail any managed identity issues explain the exact fix. disaply all this in the header bar error text
